# 倍增门白模型进度

更新时间：2026-09-09

## 本轮完成

- 已将白模职业扩展为战士、弓手、盾卫、术士四种职业，职业展示、颜色、完成行为和驻场类型集中在 `scripts/core/role_catalog.gd`。
- 首组职业门通过玩家与门的物理接触决定初始路线。
- 每职业累计 5 个零件完成角色；正数溢出保留，负数只扣未完成零件且最低为 0。
- 战士完成后等待 0.25 秒自动冲锋并消耗；弓手完成后整局持续存在并自动射击；核心角色同样自动攻击。
- 已实现普通、精英、精英与首领 3 个连续波次；每波 40 秒，整局目标 120 秒，波次间弹出三选一职业纹章商店。
- 职业进度、职业印记、驻场角色耐久和权重均跨节点持续到整局结束。
- 门抽样支持四职业加权随机，每组在左侧数字门主路生成一扇门；负门先扣未完成部件，再扣对应职业最后生成的驻场角色耐久。
- 术士完成时触发一次白模爆发弹，盾卫完成后进入有耐久的驻场队列；四职业进度与印记均在 HUD 展示。
- 羁绊 2/4/6 层已接入伤害计算：每达到一档独立乘以 1.30，2 枚为 1.30 倍、4 枚为 1.69 倍、6 枚为 2.197 倍；核心、战士、弓手、术士伤害均经过对应职业倍率。
- 已加入 `user://run_save.json` 本地存档，保存局内部件、印记、驻场角色耐久、权重、节点进度和随机种子；过门、商店选择、节点完成都会写盘。
- 局内状态新增队伍耐久和星币：击杀敌人、波次结算、击碎主路木桶均可获得星币；怪物抵达底部时按剩余生命扣除队伍耐久，耐久归零进入失败态。
- 商店每次展示 3 个职业纹章，选择不消耗星币；支持 1 次免费刷新，之后每次消耗 1 星币，并显示刷新前后职业权重。
- 通关后的节点流程已改为“商店选纹章 → 三张地图候选 → 选图开战”；地图候选按固定种子生成怪物生命总增幅和星币掉落总增幅，候选与当前选择均写入存档，重新进入可恢复到商店或选图界面。
- 已加入节点选择界面：启动检测到存档时显示已解锁节点，通关节点会解锁下一节点；Boss 完成后也可重新选择已解锁节点。
- 投射物命中改为真实碰撞半径加移动线段扫掠，避免高速投射物穿过敌人；手机端补充左右触控按钮和触摸拖动移动，继续保留窄屏触控回退。
- 界面改为 1080×900 宽画布中的纵向战斗场 + 右侧控制台：顶部演示标题与状态芯片、左侧战斗框、右侧移动按钮/角色控制/战斗状态/前线电报，窄视口自动退化为底部左右操作布局；轨道改为跟随相机锁定在演示框内，关卡推进只移动门、敌人和终点实体，不再拖动一张 2D 地面，配合缩放、透明度和层级形成轻量伪 3D 透视，不切换完整 3D 物理。
- 轨道视觉保留覆盖战斗框的三条宽轨道，但功能路径按 PRD 收敛为左侧数字门、右侧怪物主路；中间轨道只作为视觉缓冲，不再生成 gameplay 实体。
- 修复运行时透视更新缩进错误：门、敌人和木桶现在每帧按行进距离实时缩放、收拢和调整透明度，波次结束不再是唯一更新时机。
- 三轨地面进一步改为远窄近宽的透视梯形，梯形外保持深色留白；远处门和敌人横向位置按同一压缩比例向消失点收拢，避免远端路径比近端更宽。
- 修复实体层级与透视对齐：门/敌人始终绘制在梯形轨道之上，横向压缩改按屏幕纵深计算，避免门被地面覆盖或偏出轨道。
- 门按职业权重在左侧路径逐组生成；怪物、精英和首领全部从右侧主路生成，精英/首领沿用同一拼豆敌人表现但拥有更高血量和尺寸。
- 已从参考 ZIP 迁入 12 格拼豆敌人的拆解顺序与格子绘制逻辑、深度投影思路和角色配置字段风格；ZIP 不包含可直接复用的图片/音频资产。
- 已接入友方角色素材：`assets/roles/shield.png`、`mage.png`、`warrior.png`、`archer.png` 由用户提供的角色合图裁切并去除白底，玩家身后的已组成角色会按职业贴图显示。
- 已生成并接入 `assets/enemies/perler_blue_slime.png` 拼豆像素风史莱姆；普通/精英/首领共用该贴图，通过尺寸、透明度和血量区分，受击与死亡仍由现有 HP/碰撞逻辑控制。
- 投射物在物理查询回调期间改为延迟挂载碰撞形状，避免职业门触发攻击时出现 Godot 的 physics flush 错误。
- 所有当前演示参数集中在 `config/gameplay.cfg`，未将职业数、门数值范围、攻击间隔或冲锋延迟写死到玩法脚本。

## 结构

- `scripts/core/role_catalog.gd`：职业单一配置源，承载名称、颜色、完成行为和基础耐久。
- `scripts/core/run_state.gd`：纯局内领域状态，负责零件、完成角色、印记、驻场角色耐久、战士待冲锋数和权重。
- `scripts/core/game_session.gd`：波次定义、左门生成、商店三选一/刷新、`user://run_save.json` 存取、节点解锁和随机种子恢复。
- `scripts/gameplay/runner_level.gd`：关卡实体、物理接触、自动攻击和表现触发。
- `scripts/gates/profession_gate.gd`：职业门白模型。
- `scripts/enemies/basic_enemy.gd`：拼豆敌人贴图与 HP/受击逻辑，保留 ZIP 基线的受击拆解顺序。
- `scripts/props/reward_barrel.gd`：主路可击碎木桶奖励。

## 验证

- 已更新 `tests/test_run_state.gd`，覆盖正值溢出、四职业独立状态、战士消耗、弓手驻场、盾卫耐久扣除、术士溢出和整局权重。
- 使用 `/Users/tu/Downloads/Godot.app/Contents/MacOS/Godot` 4.7.2 执行通过：
  - `--headless --path . --script res://tests/test_run_state.gd`（exit 0）
  - `--headless --path . --quit-after 3`（exit 0，项目初始化无解析/运行时错误）
- 已验证 RunState 存档序列化和倍率计算；已在图形窗口中手动验证选关、普通节点、左门/右怪梯形战斗场、耐久/星币 HUD、拼豆敌人和木桶生成；商店刷新与存档状态由 headless 测试覆盖。
- 新增 `tests/test_game_session.gd`，验证节点解锁、节点选择和 JSON 往返；Godot 4.7.2 headless exit 0。
- 扩展 `tests/test_game_session.gd` 和 `tests/test_runner_combat.gd`，覆盖三波 120 秒闭合、难度顺序、实体初始比例与运行中透视变化。
- 扩展 `tests/test_game_session.gd`，覆盖三张地图候选、随机修正范围、商店完成前禁止选图、选图后的生命/星币修正和存档状态。
- 新增 `tests/test_projectile_hit.gd`，验证投射物命中后扣血并消耗自身。
- 新增 `tests/test_runner_combat.gd`，验证真实关卡自动攻击会让对齐敌人掉血。
- 资源导入后重新执行全部 headless 测试，四张友方贴图与史莱姆贴图均可被 Godot 预加载。
- 使用非 headless 图形模式启动主场景并正常退出（exit 0），Metal Mobile 渲染和主场景资源加载无错误；已获得 macOS 屏幕捕获与辅助功能权限并完成窗口截图、点击和键盘移动测试。

## 暂未实现

- 复杂敌人、商店完整商品池和断线恢复中的服务端同步仍未实现；当前存档为本地白模存档。
- 关卡数据资源化、数据库配置接入和服务端断线恢复。
- 实际 GUI 自动化回归脚本。
- 与 PRD V2.0 的主要差距：当前每组只生成一扇左侧数字门，尚未恢复“左右二选一且同组职业/数值互斥”；5 个部件仍以数值进度表现，缺少头/身/手/脚的逐件动画；商店已能调权重但尚未实现“持续 2 节点 +20”商品、连续 3 组未出现最高权重职业后的第 4 组保底；盾卫/术士的 2/4/6 层专属羁绊效果仍以通用伤害倍率代替；集装箱分裂小门、Boss 分阶段施压、关键埋点尚未完成。
